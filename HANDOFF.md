# 交接说明（Handoff）

> 此文件供 Mac mini 上的 Kimi 会话读取，帮助其快速接管本项目。
> 如果你是在新机器/新会话中读到这个文件：请先通读本文件，再读 `docs/01` 和 `docs/02`，然后等待用户指令开工。

## 项目一句话

自研"小而美 IT 服务台（企微版）"：事件管理 + 问题管理 + 简版知识库，用户体系完全绑定企业微信。对标 ManageEngine ServiceDesk Plus 做的减法设计。

## 当前状态

- ✅ 竞品调研完成（`research/`，含 SDP 真实账号上机实测 28 项裁决）
- ✅ MVP 产品设计方案完成（`docs/01-MVP产品设计方案.md`）
- ✅ 开发计划完成（`docs/02-开发计划.md`，9 轮迭代 R0–R9）
- ⬜ 代码未开始 —— **下一步是 R0（项目骨架与 Docker 环境）**

## 技术栈（用户已确认）

NestJS + Vue3 + PostgreSQL + Prisma + Redis，Docker Compose 部署。
- 前端双应用：web-h5（Vant 4，企微端）、web-admin（Element Plus，管理端）
- 代码根目录：`app/`（monorepo），文档在 `docs/`，调研在 `research/`

## 开发约定（用户硬性要求）

1. 单文件 ≤1000 行（800 行触发拆分预警）；每轮交付 ≤1000 行
2. 注释规范见 `docs/02-开发计划.md` 第二节（文件头 + 业务规则引用设计章节 + JSDoc）
3. 小颗粒迭代：按 R0→R9 顺序，每轮可运行、可验收，用户核查通过才进下一轮
4. 每轮收尾必须输出：变更文件清单+行数、核查要点、复现步骤

## 开发环境（Mac mini）

- 需要 Docker（Docker Desktop for Mac 或 colima），R0 起依赖
- 企微账号对接在 R7 才需要，之前全部用 Mock 用户开发
- 部署目标：用户的公网服务器（R9），本机只做开发

## 文档索引

| 文件 | 内容 |
|------|------|
| `docs/01-MVP产品设计方案.md` | 功能范围、状态机、字段模型、页面清单 —— 开发的功能蓝本 |
| `docs/02-开发计划.md` | R0–R9 轮次拆解、文件级行数预算、验收标准 —— 开发的执行蓝本 |
| `research/00-调研消化总报告.md` | 竞品对标与差异化依据（需要设计决策依据时查） |
| `research/hands-on/00-实测总结.md` | SDP 实测裁决（竞品事实以此为准） |

## 开工口令

用户确认环境后："开始 R0"。按 `docs/02-开发计划.md` R0 节的文件清单执行。
